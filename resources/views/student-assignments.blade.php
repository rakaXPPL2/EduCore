<!DOCTYPE html><html lang="id"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>EduCore | Tugas kelas</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body><main class="main-content" style="max-width:1100px;margin:auto"><header class="topbar"><div class="breadcrumb"><a href="{{ route('student.dashboard') }}">Portal siswa</a> / <strong>Tugas kelas</strong></div><a class="view-all" href="{{ route('student.dashboard') }}">Dashboard</a></header><section class="welcome"><div><div class="eyebrow">{{ auth()->user()->schoolClass?->name ?: 'KELAS' }}</div><h1>Tugas kelas<span style="color:#4f8df5;">.</span></h1><p>Tugas dari guru mapel untuk kelasmu.</p></div></section>@if(session('success'))<div class="success-banner">{{ session('success') }}</div>@endif<div class="data-list">@forelse($assignments as $assignment)@php($submission=$assignment->submissions->first())<section class="panel assignment-detail-card {{ $submission?->status === 'late' ? 'grade-danger' : (!$submission ? 'grade-warning' : '') }}"><div class="panel-heading"><div><span class="eyebrow">{{ $assignment->subjectModel?->name }}</span><h2>{{ $assignment->title }}</h2><p>{{ $assignment->teacherUser?->name ?: $assignment->teacher }} &bull; deadline {{ $assignment->due_at->format('d M Y H:i') }}</p></div><span class="status {{ $submission && $submission->status !== 'late' ? 'done' : '' }}">{{ $submission?->status ?: 'Belum dikumpulkan' }}</span></div><p class="detail-copy">{{ $assignment->description }}</p><form class="permit-form" action="{{ route('student.assignments.submit',$assignment) }}" method="POST" enctype="multipart/form-data">@csrf<div class="auth-form" style="grid-template-columns:1fr 1fr;gap:10px"><label class="form-label">File tugas<input class="form-control file-control" type="file" name="file"></label><label class="form-label">Foto bukti<input class="form-control file-control" type="file" name="photo" accept="image/*"></label></div><label class="form-label">Link tugas<input class="form-control" type="url" name="link" placeholder="https://..." value="{{ $submission?->link }}"></label><button class="primary-button" type="submit">{{ $submission ? 'Kirim ulang tugas' : 'Kumpulkan tugas' }}</button></form></section>@empty<div class="empty-state">Belum ada tugas untuk kelasmu.</div>@endforelse</div></main></body></html>
