<!DOCTYPE html><html lang="id"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>EduCore | Penilaian</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body><main class="main-content" style="max-width:1200px;margin:auto"><header class="topbar"><div class="breadcrumb"><a href="{{ route('teacher.assignments') }}">Kelola tugas</a> / <strong>Penilaian</strong></div><a class="view-all" href="{{ route('teacher.assignments') }}">Kembali</a></header><section class="welcome"><div><div class="eyebrow">GRADEBOOK KELAS</div><h1>{{ $assignment->title }}<span style="color:#4f8df5;">.</span></h1><p>{{ $assignment->schoolClass?->name }} &bull; {{ $assignment->subjectModel?->name }} &bull; {{ $students->count() }} murid</p></div></section>@if(session('success'))<div class="success-banner">{{ session('success') }}</div>@endif<section class="panel page-panel"><div class="panel-heading"><div><h2>Daftar pengumpulan</h2><p>Kuning = belum mengumpulkan. Merah = terlambat dan tidak mendapat nilai.</p></div></div><div class="data-list">@foreach($students as $student)@php($submission = $student->submissions->first())<div class="data-row grade-row {{ !$submission ? 'grade-warning' : ($submission->status === 'late' ? 'grade-danger' : '') }}"><div class="avatar">{{ strtoupper(substr($student->name,0,2)) }}</div><div class="row-main"><h3>{{ $student->name }}</h3><p>@if(!$submission)Belum mengumpulkan tugas @elseif($submission->status === 'late')Terlambat &bull; Tidak mendapatkan nilai @else{{ $submission->submitted_at?->format('d M Y H:i') }}@endif</p></div>@if($submission)<form class="grade-form" action="{{ route('teacher.submissions.grade',$submission) }}" method="POST">@csrf @method('PATCH')<input class="form-control score-input" type="number" name="score" value="{{ $submission->status === 'late' ? '' : $submission->assignment->grades()->where('student_id',$student->id)->value('score') }}" placeholder="0-{{ $assignment->max_points }}" min="0" max="{{ $assignment->max_points }}"><input class="form-control" name="feedback" placeholder="Feedback"><button class="view-all" type="submit">Simpan nilai</button></form>@else<span class="status">Belum ada nilai</span>@endif</div>@endforeach</div></section></main></body></html>
