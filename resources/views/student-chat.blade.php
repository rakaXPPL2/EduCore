<!DOCTYPE html><html lang="id"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>EduCore | Chat guru</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body><main class="main-content" style="max-width:900px;margin:auto"><header class="topbar"><div class="breadcrumb"><a href="{{ route('student.dashboard') }}">Portal siswa</a> / <strong>Chat guru</strong></div><a class="view-all" href="{{ route('student.dashboard') }}">Dashboard</a></header><section class="welcome"><div><div class="eyebrow">KELAS {{ auth()->user()->schoolClass?->name }}</div><h1>Chat guru mapel<span style="color:#4f8df5;">.</span></h1><p>Pilih mapel untuk bertanya kepada guru yang mengajar kelasmu.</p></div></section>@if(session('success'))<div class="success-banner">{{ session('success') }}</div>@endif<section class="panel page-panel"><div class="data-list">@forelse($messages as $message)<div class="chat-line"><div class="avatar">{{ strtoupper(substr($message->sender->name,0,2)) }}</div><div><strong>{{ $message->sender->name }} <small>{{ $message->subject->name }}</small></strong><p>{{ $message->body }}</p></div></div>@empty<div class="empty-state">Belum ada percakapan. Mulai bertanya pada guru mapelmu.</div>@endforelse</div><form class="permit-form" action="{{ route('student.chat.send') }}" method="POST" style="margin-top:20px">@csrf<input type="hidden" name="school_class_id" value="{{ auth()->user()->school_class_id }}"><label class="form-label">Mata pelajaran<select class="form-control" name="subject_id" required>@foreach($subjects as $subject)<option value="{{ $subject->id }}">{{ $subject->name }}</option>@endforeach</select></label><label class="form-label">Pesan<textarea class="form-control" name="body" rows="4" placeholder="Tulis pertanyaanmu..." required></textarea></label><button class="primary-button" type="submit">Kirim ke guru mapel</button></form></section></main></body></html>
